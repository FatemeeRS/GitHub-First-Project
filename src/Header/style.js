import styled from "styled-components";
import background from "./../acsios/background.png"

export const StyledHeader = styled.div`
  background-image: url(${background});
  background-repeat: no-repeat;
  background-size: cover;
  height: 100vh;
  width: 100%;
`;
